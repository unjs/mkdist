import unjs from "eslint-config-unjs";

export default unjs({
  ignores: [
    '**/dist/**',
  ],
  rules: {
    // rule overrides
  },
  markdown: {
    rules: {
      // markdown rule overrides
    },
  },
});
